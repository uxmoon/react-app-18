// PascalCasing, capitalize first letter of each word
function Message() {
  // JSX, JavaScript XML
  const name = 'Moon'
  if (name)
    return <h1>Hello {name}</h1>
  return <h1>Hello user!</h1>
}

export default Message;